"""Plan-vs-Actual Reconciliation & Tolerance Check.

A structured-data reconciliation engine: it compares a plan/spec
(required items with expected values and tolerances) against an
inspection/actual log (what was recorded), and produces a
reconciliation report of matches, missing items, extra items and
out-of-tolerance deviations.

See README.md for the honesty boundary / limitations of this tool.
"""
